services:
  control-plane:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
        GIT_DATE: ${GIT_DATE:-unknown}
        GIT_MESSAGE: ${GIT_MESSAGE:-unknown}
    restart: unless-stopped
    env_file:
      - ${SILO_ENV_FILE:-.env}
    environment:
      PORT: "3000"
      DISK_CACHE_DIR: "/var/cache/silo-dataplane"
      DATAPLANE_URL: "http://dataplane:3001"
      DASHBOARD_ORIGIN_DOMAINS: "${DASHBOARD_ORIGIN_DOMAINS:-primary-dashboard-origin.onsilo.dev}"
      REDIS_URL: "redis://:${REDIS_PASSWORD:?Set REDIS_PASSWORD in the deployment environment}@silo-dragonfly:6379"
    depends_on:
      silo-dragonfly:
        condition: service_started
    volumes:
      - dataplane-cache:/var/cache/silo-dataplane:ro
    # PostgreSQL is external (Aiven) and is injected through the deployment
    # environment. Keep Dokploy networking only for the current app stack.
    networks:
      - default
      - dokploy-network

  # Private, in-stack Redis-compatible cache. It is intentionally not exposed
  # on a host port: the control-plane reaches it over Docker's default network.
  silo-dragonfly:
    image: docker.dragonflydb.io/dragonflydb/dragonfly:latest
    restart: unless-stopped
    ulimits:
      memlock: -1
    command:
      - --requirepass=${REDIS_PASSWORD:?Set REDIS_PASSWORD in the deployment environment}
      - --maxmemory=8gb
      - --cache_mode=true
    volumes:
      - dragonflydata:/data

  dataplane:
    build:
      context: ./dataplane
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ${SILO_ENV_FILE:-.env}
    environment:
      DATAPLANE_BIND: "0.0.0.0:3001"
      CONTROL_PLANE_URL: "http://control-plane:3000"
      DATAPLANE_PUBLIC_SCHEME: "https"
      DATAPLANE_WRITER_INSTANCE_ID: "primary"
      DATAPLANE_WRITER_AUTO_CLAIM: "true"
      DATAPLANE_ORIGIN_DOMAINS: "${DATAPLANE_ORIGIN_DOMAINS:-primary-origin.onsilo.dev}"
      DISK_CACHE_DIR: "/var/cache/silo-dataplane"
      REDIS_URL: "redis://:${REDIS_PASSWORD:?Set REDIS_PASSWORD in the deployment environment}@silo-dragonfly:6379"
    depends_on:
      control-plane:
        condition: service_healthy
    volumes:
      - dataplane-cache:/var/cache/silo-dataplane

volumes:
  dataplane-cache:
  dragonflydata:

networks:
  dokploy-network:
    external: true

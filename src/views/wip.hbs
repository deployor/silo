<div class="flex-1 flex items-center justify-center p-6">
  <div class="max-w-md w-full bg-black/30 rounded-2xl border border-white/10 p-6 text-center">
    <h1 class="text-2xl font-bold text-white mb-3">You shall not pass</h1>

    <p class="text-text-muted mb-6">
      Ask Deployor for a key. Silo is WIP.
    </p>

    {{#if error}}
      <div class="bg-red-500/10 border border-red-500/20 p-3 rounded-lg mb-4 text-left">
        <p class="text-red-400 text-sm font-bold">{{error}}</p>
      </div>
    {{/if}}

    <form action="/auth/wip" method="POST" class="space-y-4">
      <div>
        <input
          type="password"
          name="code"
          placeholder="Enter access code"
          class="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white placeholder-text-muted focus:outline-none focus:border-hc-red transition-colors"
          required
        />
      </div>
      <button
        type="submit"
        class="w-full bg-hc-red hover:bg-hc-red/90 text-white font-bold py-3 px-4 rounded-lg transition-colors"
      >
        Frfr we coolio?
      </button>
    </form>
  </div>
</div> 
